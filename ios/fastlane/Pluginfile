# Fastlane plugins required by the iOS lanes.
# Run `bundle exec fastlane install_plugins` after editing.

# Slack notifications use the built-in slack action; no plugin needed.
# Sigstore attestation calls cosign directly via sh; no plugin needed.
