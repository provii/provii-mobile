# Appfile , Provii wallet Android.
#
# Single-bundle architecture: one package id `app.provii.wallet` for every
# audience. Sandbox vs production is a runtime toggle, not a separate package.
# (Phase 4 Decisions 6 + 9.)

package_name("app.provii.wallet")
